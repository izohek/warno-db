/// Warno Division definition
export interface Division {
    name: string
    country: string
    alliance: string
    id: number
    descriptor: string
    tags?: string[]
}

export default Division
